# Phase 1 — Kalshi client (runtime)
requests>=2.31.0,<3
cryptography>=42.0.0
python-dotenv>=1.0.0

# Dev / test
pytest>=7.4.0,<9
responses>=0.25.0

# Phase 1 — data & models (install when those modules land)
# pandas>=2.0.0
# numpy>=1.24.0
# statsmodels>=0.14.0
# scikit-learn>=1.3.0
